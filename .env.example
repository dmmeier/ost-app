# Database (SQLite is the default — no configuration needed)
# Uncomment to use PostgreSQL instead:
# DATABASE_URL=postgresql://user:password@localhost:5432/ost

# Anthropic API key (optional — only needed for AI chat features)
# Get one at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...
